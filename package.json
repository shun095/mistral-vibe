{
  "name": "mistral-vibe",
  "version": "1.0.0",
  "description": "Mistral Vibe - CLI coding agent",
  "private": true,
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "test:e2e": "playwright test --project=chromium",
    "test:e2e:ui": "playwright test --ui --project=chromium",
    "test:e2e:debug": "playwright test --debug --project=chromium",
    "test:e2e:headed": "playwright test --headed --project=chromium",
    "test:e2e:all": "playwright test",
    "test:e2e:coverage": "COVERAGE=1 npm run test:e2e && node scripts/e2e-coverage.js",
    "playwright:install": "playwright install --with-deps",
    "playwright:show-report": "playwright show-report"
  },
  "devDependencies": {
    "@babel/core": "^7.24.0",
    "@babel/preset-env": "^7.24.0",
    "@playwright/test": "^1.58.2",
    "@types/node": "^25.5.0",
    "babel-jest": "^29.7.0",
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0",
    "nyc": "^18.0.0",
    "typescript": "^6.0.2",
    "v8-to-istanbul": "^9.3.0"
  }
}
